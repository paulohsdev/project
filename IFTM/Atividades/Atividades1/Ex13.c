#include <stdio.h>
#include <stdlib.h>
#include <locale.h>

int main()
{

    int n1, contador, resultado;

    printf("Tabuada do 1:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 1;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 2:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 2;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 3:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 3;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 4:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 4;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 5:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 5;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 6:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 6;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 7:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 7;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 8:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 8;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 9:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 9;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }

    printf("Tabuada do 10:\n\n");

    for (contador = 1; contador <= 10; contador++)
    {
        n1 = 10;
        resultado = n1 * contador;
        printf("%d x %d = %d\n", n1, contador, resultado);
    }
    return 0;
}